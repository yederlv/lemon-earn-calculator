/**
 * Estilos para el componente Calculator
 * 
 * Define los estilos específicos para el contenedor del formulario de cálculo y el botón.
 */
export const calculatorStyles = {
    container: {
        padding: 2,
        backgroundColor: '#f5f5f5',
        borderRadius: '8px',
        boxShadow: '0 4px 8px rgba(0, 0, 0, 0.1)'
    },
    button: {
        marginTop: 2
    }
};
